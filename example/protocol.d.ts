/** A sample type that the main thread will send to the worker as request data. */
export type RequestPayload = number;
/** A sample type that the worker thread will respond with. */
export type ResponsePayload = string;
